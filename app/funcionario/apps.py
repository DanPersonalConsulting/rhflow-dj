from django.apps import AppConfig


class FuncionarioConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'app.funcionario'
    verbose_name = '2. Adminstrador da Org.'
